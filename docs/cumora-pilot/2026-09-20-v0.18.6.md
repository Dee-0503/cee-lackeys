# Cumora v0.18.6 升级记录

## Observed

- 部署时间 2026-09-20T03:44:01Z，目标 cee-server 的 cumora-pilot。
- 官方 tag v0.18.6 → commit `1a82fe6383fe02a18c643fafedf61d4a0d1a4439`。
- 根 package.json 和 package-lock.json 均为 0.18.6；zh-CN 文档/locale、官方 Dockerfile 与构建输入存在；server/package.json 仍不存在，沿用根 package。
- 源码 tar SHA-256：`4f9e69d1b7ee15c22673c1b2f91922dbede1661c8738080023497555c05b13a0`。
- 最终 image ID/digest：`sha256:be4c5da6a316be975d87b1e7d7d857a60ab1e6ff3b0cc8e543f3dd7192a5ec03`。
- 标签：`cumora-pilot/server:v0.18.6-1a82fe6383fe`，Compose 引用固定 digest。
- Dockerfile、db/migration 相比 0.18.5 无变更；源码未修改。
- 首次构建 npm ci 下载遇 ECONNRESET，失败日志保留 build-attempt1/；原样重试成功，TypeScript/Vite build 通过。原有 Node engine/依赖弃用/大 bundle 警告仍存在。
- 停机备份 `/home/cee/backups/cumora-pilot/snapshot.kdpqVa7I`，四项归档 SHA 校验全部通过，pg_restore --list 成功。
- migration 成功退出 0；server 容器内 package.json 实测 0.18.6；server/Postgres/Redis healthy。
- 公网 HTTPS SPA 200，/api/health ok，/api/auth/providers 仅 github，匿名 /api/auth/me 401，waitlist_enabled=true。
- 升级前后 users=2（含 seed）、user_identities=1、conversations=9、messages=0，注册门禁保留。

## Inferred

固定版本升级已完成，已注册账号保留。此次没有数据库结构变更，因此失败回滚可先恢复旧固定镜像；仍保留冷备以防数据问题。

## Unknown

用户此前已确认 0.18.5 能人工登录并到达配对页；0.18.6 升级后的人工登录与配对尚待用户确认。Agent 引擎、真实任务去重、中文 UI 全覆盖、空目录恢复仍未验证。pgvector 降级限制仍在。

## Changed

- 服务器新增 `/home/cee/apps/cumora-pilot/releases/v0.18.6/`：source.tar、source/、build/、build-attempt1/、build-v0.18.6.sh、upgrade-v0.18.6.sh、compose.previous.yaml、compose.next.yaml。
- 更新项目 compose.yaml，仅将 Cumora 镜像引用切换到新 digest；重建 server/migration 容器；Redis 因冷备停止后重启，Postgres 保持运行。
- 新增上述备份与新镜像/构建缓存，旧镜像仍保留。
- 本地更新 compose.yaml，新增构建/切换脚本和本报告；未提交 Git。

## Not changed

未安装 Codex 或 cc-switch，未修改 Agent 配置。模型地址/主辅助模型、密钥、DNS/Nginx/Cloudflare、cee-wiki、TREK、Obsidian、claude-mem、本地文件证据 MCP、qmd、外部通知渠道均未改动。

## Human acceptance

刷新 https://lackeys.ceee.cloud 并确认原 GitHub 登录及“设置你的计算机”页面可正常使用；不执行 @latest 配对命令。本次授权仅升级 Cumora，Codex 安装/配对另行推进。

## Rollback

旧 digest：`sha256:8020276ca6017d0220d3226f4e356f83375ad9720f9ea9de2c67aaf70cf70d28`。

如需回滚，在 cee-server 执行：

```sh
sudo docker compose -p cumora-pilot -f /home/cee/apps/cumora-pilot/compose.yaml stop server
sudo install -m 644 /home/cee/apps/cumora-pilot/releases/v0.18.6/compose.previous.yaml /home/cee/apps/cumora-pilot/compose.yaml
sudo docker compose -p cumora-pilot -f /home/cee/apps/cumora-pilot/compose.yaml up -d --wait server
```

数据恢复使用 snapshot.kdpqVa7I，在独立恢复目录验证后决定切换；本轮没有执行恢复或删除。停止整个 pilot 使用同作用域 compose stop。不得 prune 或清理其他项目。

## Adoption recommendation

`pilot-needs-fix`（引擎与完整验收待完成，不表示此次升级失败）。configured/built/running/healthy：是；manually-verified：仅用户已确认升级前登录；pilot-passed/adopted：否。

## Codex 与 cc-switch

cc-switch 不是 Codex 必需依赖。未来可在专用宿主环境直接配置 provider；Cumora 的受限 Codex 适配会忽略部分用户配置，因此需要针对其凭据传递与配置白名单单独验证，不能假定安装 cc-switch 就能自动适配。
