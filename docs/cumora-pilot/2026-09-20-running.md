# Cumora pilot 应用启动记录

## Observed

- 固定 v0.18.5 镜像 `sha256:8020276ca6017d0220d3226f4e356f83375ad9720f9ea9de2c67aaf70cf70d28` 已启动；server、PostgreSQL、Redis healthy，migration 退出 0，schema 版本 9。
- server 仅映射 127.0.0.1:15181；Nginx 公网灰云 HTTPS 提供 https://lackeys.ceee.cloud 。SPA 返回 200、/api/health 返回 ok、匿名 /api/auth/me 返回 401。
- /api/auth/providers 仅 github；/api/public/signup-config 返回 waitlist_enabled=true。
- 启动前 users 和 user_identities 均为 0。官方 waitlist 门禁已启用，CUMORA_ADMIN_EMAILS 仅 cyyyyy0503@gmail.com；其他新用户只可入候补，不获会话。此机制不拦截已有账户，因此后续不能批准其他候补或关闭门禁。GitHub 优先使用已验证主邮箱，不匹配会进入候补。
- 应用已重启并恢复健康，合成数据库标记及 waitlist 设置保持一致；停止/启动后再次 migration 成功，未测试真实任务重复执行。
- 已生成包含 schema 的备份 `/home/cee/backups/cumora-pilot/snapshot.5mEsc6RC`，含 Postgres dump、Redis 冷备、uploads、非秘密配置及校验清单。
- pgvector 不可用，官方 migration 自动降级，语义记忆暂不可用。

## Inferred

核心 Web/API 单机 pilot 已能运行。健康状态不说明 Agent engine 已接入，也不说明登录回调、中文回复或人工 UI 已通过。

## Unknown

真实 GitHub 登录、第三方模型完整 Responses 流式/工具兼容性、BYOA、中文 Agent 回复、日历任务、完整空目录恢复仍未验证。本人登录门禁依据官方源码与数据库状态核验，未用其他真实 GitHub 账户执行负向登录。

## Changed

runtime.env 更新用户提供的 API 地址、主模型 gpt-6-astra、辅助模型 gpt-5.6-terra、GitHub client ID 和唯一管理员邮箱；仅检查 secret 文件元信息，未输出值。执行 migration 1–9、启用 waitlist、创建 migration/server 容器和 egress 网络，官方 seed 产生示例数据。源码、官方 Dockerfile 不变。

## Not changed

cee-wiki、TREK、cc-switch、Agent CLI/配置、Obsidian、claude-mem、本地文件证据 MCP、qmd、外部通知均未接入。此次没有修改 DNS/Nginx/Cloudflare。

## Human acceptance

用户现在可打开 https://lackeys.ceee.cloud ，点击 GitHub 登录；GitHub 已验证主邮箱需匹配 cyyyyy0503@gmail.com。确认成功后测试简体中文。若出现 BYOA 配对页，不执行其 npx cumora@latest 默认命令，另行处理固定版本 engine 接入。预检报告的完整人工验收清单仍适用。

## Rollback

停止命令：`sudo docker compose -p cumora-pilot -f /home/cee/apps/cumora-pilot/compose.yaml stop`。重启用同作用域 `up -d --wait server`。数据库备份及空目录恢复方案见前序报告；不要直接用旧镜像跨 schema 回滚，不删除任何目录。公网入口独立回滚方案见 direct-domain 报告。

## Adoption recommendation

`pilot-needs-fix`（待人工验收与 engine/恢复验证）。configured、built、running、healthy：是；manually-verified、pilot-passed、adopted：否。
