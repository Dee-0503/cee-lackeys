# Project contribution notes

- Commit messages use Conventional Commits: `type(scope): subject` when a scope is useful.
- Keep deployment secrets, generated packages, caches and local tool state out of Git.
- Do not use `--no-verify` or amend commits unless the user explicitly requests it.
